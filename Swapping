#include<stdio.h>
int main()
{
	int a,b,temp;
	int *ptr1,*ptr2;
	printf("enter a and b:");
	scanf("%d%d",&a,&b);
	printf("\n before swapping a=%d and b=%d",a,b);
	ptr1=&a;
	ptr2=&b;
	temp=*ptr1;
	*ptr1=*ptr2;
	*ptr2=temp;
	printf("\n after swapping a=%d and b=%d",a,b);
	return 0;
}
